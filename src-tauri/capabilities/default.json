{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "startup-page",
  "description": "Permissions for the bundled startup and failure page only.",
  "windows": [
    "main",
    "update-toast"
  ],
  "permissions": [
    "core:default"
  ]
}
